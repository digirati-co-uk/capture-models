{
  "name": "@capture-models/helpers",
  "version": "0.3.0",
  "license": "MIT",
  "main": "dist/umd/@capture-models/helpers.js",
  "module": "lib/index.js",
  "typings": "lib/index.d.ts",
  "files": [
    "lib",
    "dist"
  ],
  "sideEffects": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/digirati-co-uk/capture-models.git",
    "directory": "packages/helpers"
  },
  "scripts": {
    "build": "fesk-build --cjs --umd && cp ./src/invalid-babel-types.d.ts ./lib/invalid-babel-types.d.ts && tsc -p ./ --emitDeclarationOnly",
    "start": "fesk-start --cjs --umd --noServer",
    "build-types": "tsc -p ./ --emitDeclarationOnly",
    "prepare": "NODE_ENV=production fesk-build --umd --cjs && tsc -p ./ --emitDeclarationOnly"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@capture-models/plugin-api": "^0.3.0",
    "@capture-models/types": "^0.3.0",
    "fast-copy": "^2.0.4",
    "immer": "^4.0.1",
    "react": "^16.10.2",
    "uuid": "^3.4.0"
  },
  "fesk": {
    "typescript": true
  },
  "devDependencies": {
    "@fesk/scripts": "1.4.0-pr.a95f698d",
    "@types/jest": "^25.1.2",
    "@types/react": "^16.9.9",
    "@types/uuid": "^3.4.7"
  }
}
