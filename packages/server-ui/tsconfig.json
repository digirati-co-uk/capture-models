{
  "extends": "../../tsconfig.json",
  "include": ["src/**/*"],
  "compilerOptions": {
    "rootDir": "src",
    "noEmitOnError": true,
    "declarationDir": "lib"
  },
  // Dependencies must be declared in references.
  "references": [{ "path": "../plugin-api" }, { "path": "../types" }]
}
