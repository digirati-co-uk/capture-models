{
  "extends": "../../tsconfig.json",
  "include": [
    "src/**/*"
  ],
  "compilerOptions": {
    "rootDir": "./src",
    "declarationDir": "./lib"
  },
  // Dependencies must be declared in references.
  "references": [
    { "path": "../editor" },
    { "path": "../plugin-api" },
    { "path": "../types" }
  ]
}
