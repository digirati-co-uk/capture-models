import React from 'react';

type Props = {};

// With field type selector.
// Remember to add type-switcher and key switcher for field editor.

export const FieldCreator: React.FC<Props> = () => {
  return <div>FieldCreator</div>;
};
