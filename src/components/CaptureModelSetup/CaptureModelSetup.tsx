import React from 'react';

type Props = {};

// Upload existing capture model
// Capture model from URL
// Brand new capture model
// Capture model from localstorage?
// Create next component screen

export const CaptureModelSetup: React.FC<Props> = () => {
  return <div>CaptureModelSetup</div>;
};
