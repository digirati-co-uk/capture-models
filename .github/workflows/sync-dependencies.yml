name: Sync dependencies

on:
  schedule:
    # 03:00 each Saturday
    - cron: '0 3 * * 6'

  workflow_dispatch: {}

jobs:
  rebase:
    runs-on: ubuntu-latest
    steps:
      - uses: DeeDeeG/rebase-github-action@main
        with:
          head_branch: master
          base_ref: dependencies
